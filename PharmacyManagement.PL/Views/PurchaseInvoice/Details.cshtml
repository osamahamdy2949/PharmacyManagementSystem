@model PurchaseInvoiceViewModel
@{ ViewData["Title"] = "Purchase Invoice"; }
<div class="page-header d-flex justify-content-between align-items-center">
    <h2>Purchase Invoice #@Model.Id</h2>
    <div>
        <a asp-controller="PurchaseReturn" asp-action="Create" asp-route-invoiceId="@Model.Id" class="btn btn-outline-warning"><i class="bi bi-arrow-return-right"></i> Create Return</a>
        <a asp-action="DownloadReceipt" asp-route-id="@Model.Id" class="btn btn-pharmacy"><i class="bi bi-file-pdf"></i> Download receipt</a>
    </div>
</div>
<p><strong>Supplier:</strong> @Model.SupplierName | <strong>Date:</strong> @DisplayHelpers.Date(Model.InvoiceDate) | <strong>Total:</strong> @DisplayHelpers.Egp(Model.TotalAmount)</p>
<div class="content-card">
<table class="table table-pharmacy">
    <thead><tr><th>Medicine</th><th>Qty</th><th>Unit</th><th>Unit Price</th><th>Subtotal</th></tr></thead>
    <tbody>
    @foreach (var item in Model.Items)
    {
        <tr>
            <td>@item.MedicineName <small class="text-muted">@item.SerialNumber</small></td>
            <td>@item.Quantity</td><td>@item.UnitLabel</td>
            <td>@DisplayHelpers.Egp(item.UnitPrice)</td>
            <td>@DisplayHelpers.Egp(item.Subtotal)</td>
        </tr>
    }
    </tbody>
</table>
</div>
<a asp-action="Index" class="btn btn-secondary">Back</a>
